# 네이버 클라우드 플랫폼 Maps API 키 ID (NCP)
VITE_NAVER_MAP_CLIENT_ID=your_ncp_key_id_here

# 백엔드 API URL (Spring Boot Gateway)
# 로컬 개발 환경
VITE_API_BASE_URL=http://localhost:8080
# Docker 개발 환경 (백엔드 컨테이너 이름 사용)
# VITE_API_BASE_URL=http://backend:8080
# EC2/Production 환경 (백엔드 서비스명 또는 호스트 IP)
# VITE_API_BASE_URL=http://openspot-backend:8080

# Google OAuth 클라이언트 ID
VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id_here

# Google OAuth 직접 로그인 URL (프론트엔드에서 직접 리다이렉트)
VITE_GOOGLE_OAUTH_URL=http://localhost:8080/oauth2/authorization/google


# 로그 레벨 설정 (DEBUG, INFO, WARN, ERROR)
# 개발 환경: DEBUG (기본값) - 모든 로그 출력
# 프로덕션 환경: ERROR (기본값) - 에러만 출력
# VITE_LOG_LEVEL=ERROR

# Docker Network 설정 가이드:
# 1. 백엔드 먼저 실행: cd ../open_spot && docker-compose up -d (openspot-network 생성)
# 2. 프론트엔드 실행: yarn docker:dev 또는 yarn docker:prod
# 3. 네트워크 통신: 컨테이너 간 서비스명으로 통신 (예: backend:8080)