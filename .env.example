# 네이버 클라우드 플랫폼 Maps API 키 ID (NCP)
NAVER_MAP_CLIENT_ID=your_ncp_key_id_here

# 백엔드 API URL (Spring Boot Gateway)
API_BASE_URL=http://localhost:8080

# Google OAuth 클라이언트 ID
GOOGLE_CLIENT_ID=your_google_oauth_client_id_here

# OAuth 리다이렉트 URI
OAUTH_REDIRECT_URI=http://localhost:3000/login

# Docker Network 설정 가이드:
# 1. 백엔드 먼저 실행: cd ../open_spot && docker-compose up -d (openspot-network 생성)
# 2. 프론트엔드 실행: yarn docker:dev 또는 yarn docker:prod
# 3. 네트워크 통신: 컨테이너 간 서비스명으로 통신 (예: backend:8080)